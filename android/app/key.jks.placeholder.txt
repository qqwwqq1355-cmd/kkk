# Keystore Placeholder Notice
# ===========================
#
# This file is a PLACEHOLDER for the Android signing keystore.
#
# IMPORTANT: DO NOT commit the actual key.jks file to version control!
#
# Instructions:
# 1. Generate a new keystore using:
#    keytool -genkey -v -keystore key.jks -keyalg RSA -keysize 2048 -validity 10000 -alias shopin
#
# 2. Store the generated key.jks file securely (not in git)
#
# 3. Configure signing credentials via one of:
#    - Environment variables: KEYSTORE_PASSWORD, KEY_ALIAS, KEY_PASSWORD
#    - gradle.properties file (not committed): Add storePassword, keyAlias, keyPassword
#    - CI/CD secrets for automated builds
#
# 4. Ensure the key.jks file is in .gitignore (already configured)
#
# For Play Store publishing:
# - Use the same keystore for all releases
# - Back up the keystore securely - losing it means you cannot update the app
# - Get SHA-1 and SHA-256 fingerprints for Firebase and assetlinks.json:
#   keytool -list -v -keystore key.jks -alias shopin
#
# See README.md Section 11 for more details.
